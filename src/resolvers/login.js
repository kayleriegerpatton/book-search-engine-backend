const { User } = require("../models");

const login = async (parent, args, context, info) => {
  //* accepts an email and password as parameters, returns an Auth type (create Auth type?)
};

module.exports = login;
