const saveBook = async (parent, args, context, info) => {
  //* accepts a book author's array, description, title, bookId, image, and link as parameters; returns a User type (use an Input type to handle the parameters)
};

module.exports = saveBook;
